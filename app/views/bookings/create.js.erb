console.log("tryna create");

$(".seat-selectable").each(function(selectableSeat){

})

createBooking(3);

function createBookingForSeat(seatID) {
  <% Booking.create(user_id: current_user.id, session_id: @session.id, discounted_price: 10, integer: 1, seat_id: seatID) %>
}
